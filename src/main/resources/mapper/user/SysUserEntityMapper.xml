<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.graint.baby.code.modules.user.dao.SysUserMapper">


    <insert id="addUser">
        insert into sys_user (id, user_name, age, email, password, test, salt)
        values (#{user.id}, #{user.userName},
                #{user.age}, #{user.email}, #{user.password}, #{user.test},#{user.salt})
    </insert>
</mapper>
